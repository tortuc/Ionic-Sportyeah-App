import { LikedNewsPipe } from './liked-news.pipe';

describe('LikedNewsPipe', () => {
  it('create an instance', () => {
    const pipe = new LikedNewsPipe();
    expect(pipe).toBeTruthy();
  });
});
