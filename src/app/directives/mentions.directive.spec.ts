import { MentionsDirective } from './mentions.directive';

describe('MentionsDirective', () => {
  it('should create an instance', () => {
    const directive = new MentionsDirective(null,null);
    expect(directive).toBeTruthy();
  });
});
