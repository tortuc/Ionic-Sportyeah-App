import { ScrollChatPipe } from './scroll-chat.pipe';

describe('ScrollChatPipe', () => {
  it('create an instance', () => {
    const pipe = new ScrollChatPipe();
    expect(pipe).toBeTruthy();
  });
});
